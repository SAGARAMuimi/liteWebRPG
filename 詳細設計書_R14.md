# 詳細設計書：R-14 マップ移動（直角構成）

作成日: 2026-03-21  
対象ブランチ: main

---

## 目次

1. [前提・スコープ](#1-前提スコープ)
2. [設計方針](#2-設計方針)
3. [マップデータ設計](#3-マップデータ設計)
   - 3.1 セル種別定義
   - 3.2 フロアマップ定義（config.py）
   - 3.3 座標系・方向定義
4. [MapManager クラス設計](#4-mapmanager-クラス設計)
   - 4.1 クラス概要
   - 4.2 メソッド仕様
5. [DBスキーマ変更](#5-dbスキーマ変更)
   - 5.1 dungeon_progress テーブル変更
   - 5.2 models/dungeon.py の変更
6. [DungeonManager の変更](#6-dungeonmanager-の変更)
   - 6.1 追加メソッド
   - 6.2 reset_progress の変更
7. [UI設計（pages/2_dungeon.py）](#7-ui設計pages2_dungeonpy)
   - 7.1 画面レイアウト
   - 7.2 移動ボタン制御
   - 7.3 session_state 設計
   - 7.4 移動処理フロー
8. [変更ファイル一覧](#8-変更ファイル一覧)
9. [テスト観点](#9-テスト観点)
10. [実装ロードマップ](#10-実装ロードマップ)

---

## 1. 前提・スコープ

### 現在のダンジョン探索システム

```
DungeonManager (game/dungeon.py)
├── resolve_event()   : 部屋に入ったときのイベント解決
├── check_encounter() : エンカウント判定
├── get_random_enemies(): 敵生成
└── get_boss()        : ボス生成

pages/2_dungeon.py
├── current_room      : 部屋番号（0〜ROOMS_PER_FLOOR）を session_state で管理
├── [次の部屋へ進む]  : ボタンで current_room をインクリメント
└── ボス部屋           : room >= ROOMS_PER_FLOOR で判定

DungeonProgress (models/dungeon.py)
└── current_floor    : 現在の階層（整数）のみ
```

### 現在の線形進行の問題点

| 項目 | 現状 | 課題 |
|---|---|---|
| 移動方式 | 線形（部屋番号順） | 探索感がなく、選択の余地がない |
| マップ表示 | なし | 現在地が視覚的に把握できない |
| 方向選択 | なし | 「どこへ行くか」という意思決定がない |
| 行き止まり/迂回 | なし | 経路選択による戦略性がない |

### スコープ

- 既存の**部屋番号ベース**の線形進行を、**X/Y座標ベース**のグリッドマップに置き換える
- 前後左右のテキストボタンで移動し、壁方向は `disabled=True`
- 現在地の周辺状況をテキストで描写する（テキストRPGのスタイルを維持）
- マップデータは `config.py` に静的定義する（DB非保存）
- 新規ファイル `game/map_manager.py` にマップロジックを分離する

### 後退互換への影響

本要件は `DungeonManager` と `pages/2_dungeon.py` の大幅な再設計を伴う。  
実装前に以下が安定していることを確認すること。

- R-02 バフ/デバフシステム
- R-04 状態異常システム
- R-05 アイテム（消耗品）
- R-08 分岐・イベントマス
- R-10 通貨・町機能
- R-11 装備システム
- R-12 / R-13 AI

---

## 2. 設計方針

### 基本方針

| 設計項目 | 採用方針 |
|---|---|
| マップ表現 | 二次元グリッド（整数配列）。`config.py` に静的定義 |
| 座標系 | `(x, y)`、左上原点。東(+x) / 南(+y) が正方向 |
| 方向表現 | 絶対方向：北 / 南 / 東 / 西（テキストRPGスタイル） |
| ボス部屋 | ゴールマス（`CELL_GOAL`）に到達時に自動起動 |
| イベント | 既存 `EventResult` / `resolve_event()` を再利用 |
| UI | テキスト描写 ＋ 4方向ボタン（ミニマップは将来拡張） |
| マップ永続化 | `dungeon_progress` の `current_x/y` で現在地のみDB保存 |

### 既存システムとの関係

```
変更前:
  2_dungeon.py → DungeonManager.resolve_event(party, room)
  room = session_state["current_room"]  (0, 1, 2, …)

変更後:
  2_dungeon.py → MapManager.move(direction)
              → DungeonManager.resolve_event_at(party, x, y)
  x, y = session_state["map_x"], session_state["map_y"]
```

---

## 3. マップデータ設計

### 3.1 セル種別定義

`config.py` に追加する定数。

```python
# R-14 セル種別
CELL_WALL    = 0  # 壁（移動不可）
CELL_PASSAGE = 1  # 通路（移動可能）
CELL_START   = 2  # スタート地点（各フロアの入場地点）
CELL_GOAL    = 3  # ボス部屋 / 次フロアへの扉（移動可能）
```

### 3.2 フロアマップ定義（config.py）

`FLOOR_MAPS: dict[int, dict]` を `config.py` に追加する。

各フロアは以下のキーで構成される。

| キー | 型 | 説明 |
|---|---|---|
| `grid` | `list[list[int]]` | 二次元配列（行=Y軸、列=X軸） |
| `start` | `tuple[int, int]` | スタート座標 `(x, y)` |
| `goal` | `tuple[int, int]` | ゴール座標（ボス部屋） `(x, y)` |
| `fixed_events` | `dict[tuple, str]` | 座標 → イベント種別（省略時はランダム） |

```python
# config.py に追加
# ─── R-14 マップ移動（直角構成）──────────────────────────────────────

# セル種別
CELL_WALL    = 0  # 壁（移動不可）
CELL_PASSAGE = 1  # 通路
CELL_START   = 2  # スタート地点
CELL_GOAL    = 3  # ボス部屋 / 次フロアへの扉

# 方向定義（name → (dx, dy)）
# 北 = Y 減少、南 = Y 増加（左上原点）
DIRECTIONS: dict[str, tuple[int, int]] = {
    "north": ( 0, -1),
    "south": ( 0,  1),
    "west":  (-1,  0),
    "east":  ( 1,  0),
}

DIRECTION_LABELS: dict[str, str] = {
    "north": "🔼 北",
    "south": "🔽 南",
    "west":  "◀️ 西",
    "east":  "▶️ 東",
}

DIRECTION_NAMES_JA: dict[str, str] = {
    "north": "北",
    "south": "南",
    "west":  "西",
    "east":  "東",
}

# セル種別 → 周辺描写テキスト
CELL_DESCRIBE: dict[int, str] = {
    CELL_WALL:    "壁が立ちはだかっている。",
    CELL_PASSAGE: "暗い通路が続いている。",
    CELL_GOAL:    "重厚な扉が見える…何かが待ち構えているようだ。",
    CELL_START:   "来た道が見える。",
}

# フロアマップ定義
# grid: [y][x] の二次元リスト（0=壁, 1=通路, 2=スタート, 3=ゴール）
# start: (x, y) スタート座標
# goal:  (x, y) ゴール座標
# fixed_events: {(x, y): event_type}  指定マスのイベント種別を固定する
FLOOR_MAPS: dict[int, dict] = {
    1: {
        "grid": [
            #  0  1  2  3  4
            [  0, 0, 0, 0, 0],  # y=0
            [  0, 2, 1, 1, 0],  # y=1  S─┬─
            [  0, 1, 0, 1, 0],  # y=2  │   │
            [  0, 1, 1, 3, 0],  # y=3  └─┴─G
            [  0, 0, 0, 0, 0],  # y=4
        ],
        "start": (1, 1),
        "goal":  (3, 3),
        "fixed_events": {
            (2, 1): "merchant",  # スタートから東に1マス
            (3, 2): "shrine",    # ゴールの1マス北
            (1, 3): "trap",      # ゴールへの南回り
        },
    },
    2: {
        "grid": [
            #  0  1  2  3  4  5
            [  0, 0, 0, 0, 0, 0],  # y=0
            [  0, 2, 1, 0, 1, 0],  # y=1  S─  ─
            [  0, 0, 1, 0, 1, 0],  # y=2    │   │
            [  0, 0, 1, 1, 1, 0],  # y=3    └─┬─┘
            [  0, 0, 0, 1, 0, 0],  # y=4      │
            [  0, 0, 0, 3, 0, 0],  # y=5      G
        ],
        "start": (1, 1),
        "goal":  (3, 5),
        "fixed_events": {
            (4, 1): "rest",    # 東の行き止まり（休憩）
            (4, 3): "chest",   # 合流地点の東（宝箱）
        },
    },
    3: {
        "grid": [
            #  0  1  2  3  4  5  6
            [  0, 0, 0, 0, 0, 0, 0],  # y=0
            [  0, 2, 1, 1, 0, 0, 0],  # y=1  S──
            [  0, 0, 0, 1, 0, 0, 0],  # y=2     │
            [  0, 0, 1, 1, 1, 1, 0],  # y=3  ───┼──
            [  0, 0, 1, 0, 0, 1, 0],  # y=4  │     │
            [  0, 0, 1, 1, 1, 1, 0],  # y=5  └───┬─┘
            [  0, 0, 0, 0, 3, 0, 0],  # y=6      G
        ],
        "start": (1, 1),
        "goal":  (4, 6),
        "fixed_events": {
            (2, 3): "shrine",    # 西ルートの分岐点
            (5, 3): "merchant",  # 東ルートの分岐点
            (2, 5): "rest",      # 南西の行き止まり
        },
    },
}
```

### 3.3 座標系・方向定義

```
   西(x-1) ←  → 東(x+1)
              ↑
           北(y-1)
              ↓
           南(y+1)
```

- グリッドは `grid[y][x]` の順でアクセスする。
- `y=0` が最北端、`y=max` が最南端。
- プレイヤーの「前後左右」ではなく絶対方向（北/南/東/西）で統一する。

---

## 4. MapManager クラス設計

新規ファイル `game/map_manager.py` に実装する。

### 4.1 クラス概要

```python
# game/map_manager.py
from __future__ import annotations
from config import (
    FLOOR_MAPS, DIRECTIONS, DIRECTION_NAMES_JA,
    CELL_WALL, CELL_DESCRIBE,
)


class MapManager:
    """
    グリッドマップ上のプレイヤー位置管理と移動ロジックを担う。

    - マップデータ（config.FLOOR_MAPS）を保持する。
    - 現在座標 (x, y) から移動可能方向を計算する。
    - 移動後の周辺テキスト描写を生成する。

    Attributes:
        floor        : 現在のフロア番号
        x            : 現在のX座標
        y            : 現在のY座標
        grid         : フロアのグリッドデータ [y][x]
        start        : スタート座標 (x, y)
        goal         : ゴール座標 (x, y)
        fixed_events : 座標 → イベント種別の固定マップ
    """

    def __init__(self, floor: int, x: int, y: int) -> None: ...
    def can_move(self, direction: str) -> bool: ...
    def available_directions(self) -> list[str]: ...
    def move(self, direction: str) -> tuple[int, int]: ...
    def is_goal(self) -> bool: ...
    def is_start(self) -> bool: ...
    def cell_at(self, x: int, y: int) -> int: ...
    def describe_surroundings(self) -> str: ...
    def get_fixed_event(self) -> str | None: ...
```

### 4.2 メソッド仕様

#### `__init__(floor, x, y)`

```python
def __init__(self, floor: int, x: int, y: int) -> None:
    floor_data = FLOOR_MAPS[floor]
    self.floor = floor
    self.x = x
    self.y = y
    self.grid: list[list[int]] = floor_data["grid"]
    self.start: tuple[int, int] = floor_data["start"]
    self.goal:  tuple[int, int] = floor_data["goal"]
    self.fixed_events: dict[tuple[int, int], str] = floor_data.get("fixed_events", {})
```

#### `can_move(direction) -> bool`

```python
def can_move(self, direction: str) -> bool:
    """指定方向へ移動できるか（通路/スタート/ゴール = True、壁/範囲外 = False）"""
    dx, dy = DIRECTIONS[direction]
    nx, ny = self.x + dx, self.y + dy
    if ny < 0 or ny >= len(self.grid) or nx < 0 or nx >= len(self.grid[0]):
        return False
    return self.grid[ny][nx] != CELL_WALL
```

#### `available_directions() -> list[str]`

```python
def available_directions(self) -> list[str]:
    """移動可能な方向名のリストを返す"""
    return [d for d in DIRECTIONS if self.can_move(d)]
```

#### `move(direction) -> tuple[int, int]`

```python
def move(self, direction: str) -> tuple[int, int]:
    """
    指定方向へ移動し、新しい座標 (x, y) を返す。
    移動不可の場合は ValueError を送出する。
    """
    if not self.can_move(direction):
        raise ValueError(f"方向 '{direction}' には移動できません。")
    dx, dy = DIRECTIONS[direction]
    self.x += dx
    self.y += dy
    return self.x, self.y
```

#### `is_goal() -> bool`

```python
def is_goal(self) -> bool:
    """現在地がゴールマス（ボス部屋）かどうか"""
    return (self.x, self.y) == self.goal
```

#### `is_start() -> bool`

```python
def is_start(self) -> bool:
    """現在地がスタートマスかどうか"""
    return (self.x, self.y) == self.start
```

#### `cell_at(x, y) -> int`

```python
def cell_at(self, x: int, y: int) -> int:
    """指定座標のセル種別を返す。範囲外の場合は CELL_WALL を返す"""
    if y < 0 or y >= len(self.grid) or x < 0 or x >= len(self.grid[0]):
        return CELL_WALL
    return self.grid[y][x]
```

#### `describe_surroundings() -> str`

```python
def describe_surroundings(self) -> str:
    """
    現在地の周辺4方向を描写するテキストを返す。

    例:
      北: 壁が立ちはだかっている。
      南: 暗い通路が続いている。
      東: 重厚な扉が見える…何かが待ち構えているようだ。
      西: 壁が立ちはだかっている。
    """
    lines = []
    for direction, (dx, dy) in DIRECTIONS.items():
        cell = self.cell_at(self.x + dx, self.y + dy)
        dir_ja = DIRECTION_NAMES_JA[direction]
        desc   = CELL_DESCRIBE.get(cell, "???")
        lines.append(f"{dir_ja}: {desc}")
    return "\n".join(lines)
```

#### `get_fixed_event() -> str | None`

```python
def get_fixed_event(self) -> str | None:
    """現在地に固定イベントがあれば event_type を返す、なければ None"""
    return self.fixed_events.get((self.x, self.y))
```

---

## 5. DBスキーマ変更

### 5.1 dungeon_progress テーブル変更

`current_x` / `current_y` カラムを追加する。  
デフォルト値は `-1`（未配置を示すセンチネル値）。  
フロア移動時またはダンジョン開始時に `start` 座標で初期化する。

```sql
-- data/db_init.sql に追加（マイグレーション SQL）
ALTER TABLE dungeon_progress ADD COLUMN current_x INTEGER NOT NULL DEFAULT -1;
ALTER TABLE dungeon_progress ADD COLUMN current_y INTEGER NOT NULL DEFAULT -1;
```

> **注意**: SQLite の `ALTER TABLE` は `ADD COLUMN` のみサポート。  
> `DEFAULT -1` は既存レコードにも適用される。  
> 実際の座標初期化は `DungeonManager.start_floor()` 時に行う。

### 5.2 models/dungeon.py の変更

`DungeonProgress` モデルに `current_x` / `current_y` を追加し、  
位置保存ヘルパーメソッド `set_position()` を追加する。

```python
# models/dungeon.py（変更箇所のみ）

class DungeonProgress(Base):
    __tablename__ = "dungeon_progress"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), nullable=False)
    dungeon_id: Mapped[int] = mapped_column(ForeignKey("dungeons.id"), nullable=False)
    current_floor: Mapped[int] = mapped_column(default=1, nullable=False)
    current_x: Mapped[int] = mapped_column(default=-1, nullable=False)  # ← 追加
    current_y: Mapped[int] = mapped_column(default=-1, nullable=False)  # ← 追加
    is_cleared: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False
    )

    # --- 既存メソッドは変更なし ---

    def set_position(self, x: int, y: int, db: Session) -> None:
        """現在地をDBに保存する"""  # ← 追加
        self.current_x = x
        self.current_y = y
        self.save(db)
```

---

## 6. DungeonManager の変更

`game/dungeon.py` に以下の変更を加える。

### 6.1 追加メソッド

```python
# game/dungeon.py（追加 import）
from game.map_manager import MapManager
from config import FLOOR_MAPS
```

#### `get_map_manager() -> MapManager`

```python
def get_map_manager(self) -> MapManager:
    """
    現在フロアの MapManager を生成して返す。
    DungeonProgress の current_x/y が -1（未初期化）の場合は
    フロアのスタート座標を使用する。
    """
    floor = self.progress.current_floor
    floor_data = FLOOR_MAPS[floor]
    sx, sy = floor_data["start"]
    x = self.progress.current_x if self.progress.current_x >= 0 else sx
    y = self.progress.current_y if self.progress.current_y >= 0 else sy
    return MapManager(floor, x, y)
```

#### `start_floor(floor) -> MapManager`

```python
def start_floor(self, floor: int) -> MapManager:
    """
    指定フロアをスタートする。
    DungeonProgress の current_floor / current_x / current_y を更新してDBに保存する。
    """
    floor_data = FLOOR_MAPS[floor]
    sx, sy = floor_data["start"]
    self.progress.current_floor = floor
    self.progress.current_x = sx
    self.progress.current_y = sy
    self.progress.save(self.db)
    return MapManager(floor, sx, sy)
```

#### `resolve_event_at(x, y, party, ...) -> EventResult`

```python
def resolve_event_at(
    self,
    x: int,
    y: int,
    party: list,
    hp_mult: float = 1.0,
    atk_mult: float = 1.0,
) -> EventResult:
    """
    座標 (x, y) のイベントを解決する。

    - ゴールマス    : 常にボス戦
    - 固定イベント  : fixed_events の event_type を使用
    - それ以外      : EVENT_WEIGHTS によるランダム選択（既存ロジック再利用）
    """
    mm = MapManager(self.current_floor, x, y)
    if mm.is_goal():
        return self._event_boss(hp_mult, atk_mult)

    fixed = mm.get_fixed_event()
    if fixed:
        return self._resolve_by_type(fixed, party, hp_mult, atk_mult)

    # ランダムイベント（既存の resolve_event を room=0 で流用）
    return self.resolve_event(party, room=0, hp_mult=hp_mult, atk_mult=atk_mult)
```

#### `_resolve_by_type(event_type, ...) -> EventResult`

```python
def _resolve_by_type(
    self,
    event_type: str,
    party: list,
    hp_mult: float,
    atk_mult: float,
) -> EventResult:
    """イベント種別を直接指定してイベント解決を行う（内部メソッド）"""
    if event_type == "encounter":
        return self._event_encounter(party, hp_mult, atk_mult)
    elif event_type == "trap":
        return self._event_trap(party)
    elif event_type == "merchant":
        return self._event_merchant()
    elif event_type == "shrine":
        return self._event_shrine(party)
    elif event_type == "rest":
        return self._event_rest(party)
    elif event_type == "chest":
        return self._event_chest()
    else:
        return EventResult(event_type="nothing", messages=["静かだ…何も起きなかった。"])
```

### 6.2 `reset_progress` の変更

リセット時に `current_x/y` も初期化する。

```python
def reset_progress(self) -> None:
    self.progress.current_floor = 1
    self.progress.current_x = -1  # ← 追加
    self.progress.current_y = -1  # ← 追加
    self.progress.is_cleared = False
    self.progress.save(self.db)
```

---

## 7. UI設計（pages/2_dungeon.py）

### 7.1 画面レイアウト

```
┌──────────────────────────────────────────────────────┐
│ 🏰 ダンジョン探索                                     │
│ 📍 暗黒の洞窟  1F / 3F         現在地: (2, 1)        │
│                          💰 GOLD: 300 G   🏘️ 町へ   │
├──────────────────────────────────────────────────────┤
│ 🧑‍🤝‍🧑 パーティ                                        │
│  [HP バー × パーティ人数]                            │
├──────────────────────────────────────────────────────┤
│ 👁️ 周辺状況                                           │
│   北: 壁が立ちはだかっている。                        │
│   南: 暗い通路が続いている。                          │
│   東: 暗い通路が続いている。                          │
│   西: 壁が立ちはだかっている。                        │
├──────────────────────────────────────────────────────┤
│                [🔼 北]                               │
│         [◀️ 西]  🧍  [▶️ 東]                         │
│                [🔽 南]                               │
│   ※ 移動不可の方向は disabled（グレーアウト）        │
├──────────────────────────────────────────────────────┤
│ 📜 探索ログ（最新 20 件）                             │
│   ▶ 南へ移動した。                                   │
│   ⚔️ スライム、コウモリが現れた！                    │
└──────────────────────────────────────────────────────┘
```

### 7.2 移動ボタン制御

`st.columns` を使った十字キーレイアウト。  
各ボタンは `MapManager.can_move()` の結果を `disabled` に渡す。

```python
# pages/2_dungeon.py（移動ボタン部分の実装イメージ）

from game.map_manager import MapManager
from config import DIRECTION_LABELS

def render_move_buttons(mm: MapManager) -> str | None:
    """
    十字ボタンを描画し、クリックされた方向名を返す。
    何もクリックされなければ None を返す。
    """
    clicked = None

    _, col_n, _ = st.columns([1, 1, 1])
    with col_n:
        if st.button(DIRECTION_LABELS["north"],
                     disabled=not mm.can_move("north"), key="btn_north"):
            clicked = "north"

    col_w, col_c, col_e = st.columns([1, 1, 1])
    with col_w:
        if st.button(DIRECTION_LABELS["west"],
                     disabled=not mm.can_move("west"), key="btn_west"):
            clicked = "west"
    with col_c:
        st.markdown("<div style='text-align:center'>🧍</div>", unsafe_allow_html=True)
    with col_e:
        if st.button(DIRECTION_LABELS["east"],
                     disabled=not mm.can_move("east"), key="btn_east"):
            clicked = "east"

    _, col_s, _ = st.columns([1, 1, 1])
    with col_s:
        if st.button(DIRECTION_LABELS["south"],
                     disabled=not mm.can_move("south"), key="btn_south"):
            clicked = "south"

    return clicked
```

### 7.3 session_state 設計

`2_dungeon.py` で管理する `session_state` キーの追加・変更。

| キー | 型 | 概要 | 変更 |
|---|---|---|---|
| `map_x` | `int` | 現在のX座標 | **新規追加** |
| `map_y` | `int` | 現在のY座標 | **新規追加** |
| `last_event_pos` | `tuple[int,int]` | 最後にイベントを解決した座標 | **新規追加** |
| `dungeon_log` | `list[str]` | 探索ログ | 既存（継続利用） |
| `floor_cleared` | `bool` | フロアクリアフラグ | 既存（継続利用） |
| `dungeon_cleared` | `bool` | 全クリアフラグ | 既存（継続利用） |
| `merchant_stock` | `list` | 商人在庫 | 既存（継続利用） |
| `show_merchant` | `bool` | 商人UI表示フラグ | 既存（継続利用） |
| `current_room` | `int` | 部屋番号 | **削除**（`map_x/y` に移行） |

> `last_event_pos` は同一マスで重複してイベントが発火しないよう使用する（1マス1回制限）。  
> ゴールマスはこの制限の対象外とし、常にボス戦が起動する。

### 7.4 移動処理フロー

```
ユーザーが方向ボタンをクリック
  │
  ├─ MapManager.move(direction) → 新座標 (nx, ny)
  ├─ session_state["map_x/y"] を更新
  ├─ DungeonProgress.set_position(nx, ny, db) → DB保存
  ├─ 移動メッセージを dungeon_log に追記
  │    例:「▶ 南へ移動した。  現在地: (2, 2)」
  │
  ├─ (nx, ny) == last_event_pos？
  │    → Yes: イベントスキップ（通過済みマス）
  │    → No:  イベント解決へ
  │
  ├─ DungeonManager.resolve_event_at(nx, ny, party, …)
  │    ├─ need_battle=True
  │    │    → session_state["battle_enemies"] を設定
  │    │    → st.switch_page("pages/3_battle.py")
  │    ├─ event_type="trap"
  │    │    → ダメージ適用 → ログ追加
  │    ├─ event_type="merchant"
  │    │    → show_merchant=True
  │    ├─ event_type="shrine" / "rest"
  │    │    → HP/MP 回復 → ログ追加
  │    └─ event_type="chest"
  │         → ゴールド/アイテム付与 → ログ追加
  │
  ├─ last_event_pos = (nx, ny) を更新
  └─ st.rerun()
```

---

## 8. 変更ファイル一覧

| ファイル | 変更種別 | 主な変更内容 |
|---|---|---|
| `config.py` | 変更 | `CELL_*` 定数、`DIRECTIONS`、`DIRECTION_LABELS`、`DIRECTION_NAMES_JA`、`CELL_DESCRIBE`、`FLOOR_MAPS` を追加 |
| `game/map_manager.py` | **新規作成** | `MapManager` クラス全体 |
| `models/dungeon.py` | 変更 | `DungeonProgress` に `current_x` / `current_y` カラムと `set_position()` を追加 |
| `game/dungeon.py` | 変更 | `get_map_manager()`、`start_floor()`、`resolve_event_at()`、`_resolve_by_type()`、`reset_progress()` を追加・変更 |
| `pages/2_dungeon.py` | 変更 | `current_room` を `map_x/y` に置換。`render_move_buttons()` 追加。移動処理フロー全体を再実装 |
| `data/db_init.sql` | 変更 | `dungeon_progress` テーブルに `current_x` / `current_y` を追加（`ALTER TABLE`） |
| `tests/test_dungeon.py` | 変更 | `MapManager` の単体テストを追加 |

---

## 9. テスト観点

### 9.1 MapManager 単体テスト

| テストケース | 確認内容 |
|---|---|
| `can_move` - 壁方向 | `False` が返ること |
| `can_move` - 通路方向 | `True` が返ること |
| `can_move` - マップ端（範囲外） | `False` が返ること |
| `move` - 通路方向 | 座標が `(dx, dy)` 分移動すること |
| `move` - 壁方向 | `ValueError` が送出されること |
| `is_goal` | ゴール座標で `True`、それ以外で `False` |
| `is_start` | スタート座標で `True`、それ以外で `False` |
| `cell_at` - 範囲外座標 | `CELL_WALL` が返ること |
| `describe_surroundings` | 4方向それぞれの描写テキストが含まれること |
| `get_fixed_event` - 固定マス | 正しいイベント種別が返ること |
| `get_fixed_event` - 固定なし | `None` が返ること |
| `available_directions` | 通路方向のみのリストが返ること |

### 9.2 DungeonManager 統合テスト

| テストケース | 確認内容 |
|---|---|
| `start_floor(1)` | `current_x/y` がスタート座標に設定されること |
| `start_floor(1)` | DBに座標が保存されること |
| `get_map_manager` - `current_x/y == -1` | スタート座標で `MapManager` が生成されること |
| `resolve_event_at(goal_x, goal_y)` | ボス戦の `EventResult` が返ること |
| `resolve_event_at(fixed_event_pos)` | 固定イベント種別の `EventResult` が返ること |
| `reset_progress` | `current_x/y` が `-1` にリセットされること |

### 9.3 UI・性能テスト

| テストケース | 確認内容 |
|---|---|
| 移動不可方向のボタン | `disabled=True` で描画されること |
| 全方向が壁のマス | 4ボタン全て `disabled=True` であること |
| 1操作あたりの応答時間 | 1秒以内（ローカル環境目安） |
| DB保存の確認 | 移動後にページリロードしても座標が維持されること |
| 同一マスの重複イベント | `last_event_pos` によりスキップされること |
| フロア移動後の初期化 | 次フロアのスタート座標から開始されること |

---

## 10. 実装ロードマップ

### ステップ 1：基盤整備（1〜2日）

1. `config.py` にセル種別・方向・マップデータを追加する。
2. `game/map_manager.py` を新規作成（`MapManager` クラス全体）。
3. `tests/test_dungeon.py` に `MapManager` の単体テストを追加して検証する。

### ステップ 2：DB・モデル変更（0.5日）

4. `data/db_init.sql` に `ALTER TABLE` 文を追加する。
5. `models/dungeon.py` の `DungeonProgress` に `current_x/y` と `set_position()` を追加する。
6. `game/dungeon.py` に `get_map_manager()`、`start_floor()`、`resolve_event_at()`、`_resolve_by_type()` を追加する。
7. `reset_progress()` を変更して `current_x/y` もリセットするようにする。

### ステップ 3：UI 実装（2〜3日）

8. `pages/2_dungeon.py` を再実装する。
   - `current_room` を `map_x/y` に置換する。
   - `render_move_buttons()` を実装する。
   - 移動 → イベント解決 → ログ更新のフローを実装する。
   - 商人・宝箱・戦闘遷移の動作を確認する。

### ステップ 4：テスト・調整（1日）

9. 統合テストを実施する。
10. マップレイアウトのバランスを調整する（行き止まり・迂回路の検証）。
11. 応答時間を計測し、必要に応じて最適化する。

### 合計工数目安: 4.5〜6.5日

---

> **実装着手タイミングの注意**  
> 本要件は `DungeonManager` と `pages/2_dungeon.py` の大幅な再設計を伴うため、  
> R-02〜R-13 の実装が安定してから最後に着手することを推奨する。  
> （改良要件定義書 §5.6 の制約に準拠）
